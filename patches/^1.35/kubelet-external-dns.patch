diff --git a/pkg/kubelet/nodestatus/setters.go b/pkg/kubelet/nodestatus/setters.go
index 4a22a830feb..8b13672a79a 100644
--- a/pkg/kubelet/nodestatus/setters.go
+++ b/pkg/kubelet/nodestatus/setters.go
@@ -21,6 +21,7 @@ import (
 	"fmt"
 	"math"
 	"net"
+	"os"
 	goruntime "runtime"
 	"strings"
 	"sync"
@@ -134,6 +135,7 @@ func NodeAddress(nodeIPs []net.IP, // typically Kubelet.nodeIPs
 			if nodeIP == nil {
 				node.Status.Addresses = []v1.NodeAddress{
 					{Type: v1.NodeHostName, Address: hostname},
+					{Type: v1.NodeExternalDNS, Address: os.Getenv("KUBELET_EXTERNAL_DNS")},
 				}
 				return nil
 			}
@@ -143,6 +145,7 @@ func NodeAddress(nodeIPs []net.IP, // typically Kubelet.nodeIPs
 				{Type: v1.NodeInternalIP, Address: nodeIP.String()},
 				{Type: v1.NodeInternalIP, Address: secondaryNodeIP.String()},
 				{Type: v1.NodeHostName, Address: hostname},
+				{Type: v1.NodeExternalDNS, Address: os.Getenv("KUBELET_EXTERNAL_DNS")},
 			}
 		} else {
 			var ipAddr net.IP
@@ -185,6 +188,7 @@ func NodeAddress(nodeIPs []net.IP, // typically Kubelet.nodeIPs
 			node.Status.Addresses = []v1.NodeAddress{
 				{Type: v1.NodeInternalIP, Address: ipAddr.String()},
 				{Type: v1.NodeHostName, Address: hostname},
+				{Type: v1.NodeExternalDNS, Address: os.Getenv("KUBELET_EXTERNAL_DNS")},
 			}
 		}
 		return nil
